{
  "name": "partviewer",
  "version": "0.0.1",
  "description": "Just a test to view a part 3D model",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "build": "rollup -c \"./scripts/rollup.config.js\"",
    "start": "http-server ./ -S --cert ~/.my_secrets/cert.pem  --key ~/.my_secrets/key.pem -o"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^13.1.3",
    "http-server": "^14.1.0",
    "mocha": "^9.1.4"
  },
  "dependencies": {
    "three": "^0.136.0"
  }
}
